<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Acceso Universitario | Sistema Bibliotecario</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap');
    
    :root {
      --primary: #1B396A;
      --primary-light: #2A4D7F;
      --primary-lighter: #3A6295;
      --primary-lightest: #E9EEF5;
      --dark: #1A1A1A;
      --gray-dark: #4A4A4A;
      --gray-medium: #7D7D7D;
      --gray-light: #E5E5E5;
      --gray-lightest: #F8FAFC;
      --white: #FFFFFF;
      --accent: #E63946;
      --success: #2E7D32;
      --border-radius: 10px;
      --box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
      --transition: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
    }
    
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }
    
    body {
      font-family: 'Inter', sans-serif;
      margin: 0;
      padding: 0;
      min-height: 100vh;
      display: grid;
      grid-template-columns: 1fr 1fr;
      background-color: var(--gray-lightest);
      color: var(--dark);
      line-height: 1.6;
    }
    
    /* Hero Section */
    .hero-section {
      background: 
        linear-gradient(135deg, rgba(27, 57, 106, 0.9) 0%, rgba(27, 57, 106, 0.95) 100%),
        url('https://images.unsplash.com/photo-1589998059171-988d887df646?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1476&q=80') center/cover no-repeat;
      color: var(--white);
      display: flex;
      flex-direction: column;
      justify-content: center;
      padding: 3rem;
      position: relative;
      overflow: hidden;
      animation: fadeIn 0.8s ease-out;
    }
    
    @keyframes fadeIn {
      from { opacity: 0; }
      to { opacity: 1; }
    }
    
    .hero-section::before {
      content: '';
      position: absolute;
      bottom: 0;
      left: 0;
      width: 100%;
      height: 100px;
      background: linear-gradient(transparent, rgba(0,0,0,0.1));
      z-index: 1;
    }
    
    .university-logo {
      position: absolute;
      top: 2.5rem;
      left: 2.5rem;
      max-width: 200px;
      height: auto;
      z-index: 2;
      filter: brightness(0) invert(1);
      transition: var(--transition);
    }
    
    .university-logo:hover {
      transform: scale(1.05);
    }
    
    .hero-content {
      max-width: 520px;
      margin: 0 auto;
      position: relative;
      z-index: 2;
    }
    
    .hero-title {
      font-size: 2.25rem;
      font-weight: 700;
      margin-bottom: 1.25rem;
      line-height: 1.3;
      position: relative;
      display: inline-block;
    }
    
    .hero-title::after {
      content: '';
      position: absolute;
      bottom: -8px;
      left: 0;
      width: 60px;
      height: 4px;
      background: var(--white);
      border-radius: 2px;
    }
    
    .hero-subtitle {
      font-size: 1.125rem;
      opacity: 0.92;
      font-weight: 400;
      margin-bottom: 2.5rem;
      line-height: 1.7;
    }
    
    .features-list {
      list-style: none;
      margin-top: 2rem;
    }
    
    .features-list li {
      margin-bottom: 1rem;
      display: flex;
      align-items: center;
      gap: 0.75rem;
      font-size: 0.95rem;
    }
    
    .features-list i {
      color: rgba(255, 255, 255, 0.9);
      font-size: 1.1rem;
    }
    
    /* Login Section */
    .login-section {
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 3rem 2rem;
      background: var(--gray-lightest);
    }
    
    .login-card {
      width: 100%;
      max-width: 440px;
      background: var(--white);
      border-radius: var(--border-radius);
      overflow: hidden;
      box-shadow: var(--box-shadow);
      border: 1px solid var(--gray-light);
      transform: translateY(0);
      transition: var(--transition);
      animation: slideUp 0.6s ease-out;
    }
    
    @keyframes slideUp {
      from { 
        transform: translateY(20px);
        opacity: 0;
      }
      to { 
        transform: translateY(0);
        opacity: 1;
      }
    }
    
    .login-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 15px 30px rgba(0, 0, 0, 0.12);
    }
    
    .card-header {
      padding: 2.25rem;
      text-align: center;
      background-color: var(--primary);
      color: var(--white);
      position: relative;
    }
    
    .card-header::after {
      content: '';
      position: absolute;
      bottom: 0;
      left: 0;
      width: 100%;
      height: 4px;
      background: linear-gradient(90deg, var(--primary) 0%, var(--primary-light) 100%);
    }
    
    .system-logo {
      font-size: 1.875rem;
      font-weight: 700;
      letter-spacing: -0.5px;
      margin-bottom: 0.75rem;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 0.5rem;
    }
    
    .system-logo i {
      font-size: 1.5rem;
      color: var(--white);
    }
    
    .system-logo span {
      font-weight: 800;
      color: var(--white);
    }
    
    .system-description {
      font-size: 0.9375rem;
      color: rgba(255, 255, 255, 0.85);
      font-weight: 400;
    }
    
    .card-body {
      padding: 2.25rem;
    }
    
    .form-group {
      margin-bottom: 1.75rem;
      position: relative;
    }
    
    .form-label {
      display: block;
      margin-bottom: 0.75rem;
      font-size: 0.875rem;
      font-weight: 600;
      color: var(--gray-dark);
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }
    
    .form-label i {
      color: var(--primary);
      font-size: 1rem;
    }
    
    .form-control {
      width: 100%;
      padding: 0.9375rem 1.25rem 0.9375rem 2.75rem;
      font-size: 0.9375rem;
      border: 1px solid var(--gray-light);
      border-radius: var(--border-radius);
      background-color: var(--white);
      transition: var(--transition);
      color: var(--dark);
    }
    
    .form-control::placeholder {
      color: var(--gray-medium);
      opacity: 0.7;
    }
    
    .form-control:focus {
      outline: none;
      border-color: var(--primary);
      box-shadow: 0 0 0 3px var(--primary-lightest);
    }
    
    .input-icon {
      position: absolute;
      left: 15px;
      top: 50%;
      transform: translateY(-50%);
      color: var(--gray-medium);
      font-size: 1rem;
    }
    
    .password-container {
      position: relative;
    }
    
    .password-container .input-icon {
      left: 15px;
    }
    
    .toggle-password {
      position: absolute;
      right: 15px;
      top: 50%;
      transform: translateY(-50%);
      cursor: pointer;
      color: var(--gray-medium);
      transition: var(--transition);
      background: none;
      border: none;
      font-size: 1rem;
    }
    
    .toggle-password:hover {
      color: var(--primary);
    }
    
    .btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      width: 100%;
      padding: 1.0625rem;
      font-size: 0.9375rem;
      font-weight: 600;
      border-radius: var(--border-radius);
      cursor: pointer;
      transition: var(--transition);
      border: none;
      letter-spacing: 0.5px;
      gap: 0.5rem;
    }
    
    .btn-primary {
      background: var(--primary);
      color: var(--white);
      box-shadow: 0 4px 6px rgba(27, 57, 106, 0.15);
    }
    
    .btn-primary:hover {
      background: var(--primary-light);
      transform: translateY(-2px);
      box-shadow: 0 6px 12px rgba(27, 57, 106, 0.2);
    }
    
    .btn-primary:active {
      transform: translateY(0);
    }
    
    .form-footer {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-top: 1.5rem;
      font-size: 0.875rem;
    }
    
    .remember-me {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      color: var(--gray-medium);
      cursor: pointer;
      transition: var(--transition);
    }
    
    .remember-me:hover {
      color: var(--gray-dark);
    }
    
    .remember-me input {
      cursor: pointer;
      accent-color: var(--primary);
    }
    
    .forgot-password {
      color: var(--primary);
      text-decoration: none;
      font-weight: 500;
      transition: var(--transition);
      display: flex;
      align-items: center;
      gap: 0.25rem;
    }
    
    .forgot-password:hover {
      color: var(--primary-light);
      text-decoration: underline;
    }
    
    .card-footer {
      padding: 1.5rem 2.25rem;
      text-align: center;
      font-size: 0.875rem;
      color: var(--gray-medium);
      border-top: 1px solid var(--gray-light);
      background-color: var(--gray-lightest);
    }
    
    .card-footer a {
      color: var(--primary);
      text-decoration: none;
      font-weight: 500;
      transition: var(--transition);
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 0.5rem;
    }
    
    .card-footer a:hover {
      color: var(--primary-light);
      text-decoration: underline;
    }
    
    /* Responsive Design */
    @media (max-width: 992px) {
      .hero-title {
        font-size: 2rem;
      }
      
      .hero-subtitle {
        font-size: 1rem;
      }
    }
    
    @media (max-width: 768px) {
      body {
        grid-template-columns: 1fr;
      }
      
      .hero-section {
        display: none;
      }
      
      .login-section {
        padding: 2rem 1.5rem;
        min-height: 100vh;
      }
      
      .login-card {
        max-width: 480px;
      }
    }
    
    @media (max-width: 480px) {
      .card-header,
      .card-body {
        padding: 1.75rem;
      }
      
      .form-footer {
        flex-direction: column;
        align-items: flex-start;
        gap: 1rem;
      }
    }
  </style>
</head>
<body>
  <!-- Sección institucional con imagen de fondo -->
  <section class="hero-section">
    <img src="" alt="" class="university-logo">
    
    <div class="hero-content">
      <h1 class="hero-title">Acceso al Sistema Bibliotecario</h1>
      <p class="hero-subtitle">Utilice sus credenciales institucionales para acceder a todos los recursos digitales y servicios exclusivos.</p>
      
      <ul class="features-list">
        <li><i class="fas fa-book-open"></i> Catálogo completo de libros digitales</li>
        <li><i class="fas fa-search"></i> Búsqueda avanzada de recursos</li>
        <li><i class="fas fa-clock"></i> Reserva de materiales</li>
      </ul>
    </div>
  </section>
  
  <!-- Sección de login -->
  <section class="login-section">
    <div class="login-card">
      <div class="card-header">
        <div class="system-logo">
          <i class="fas fa-book"></i>
          BIBLIOTECA<span>ITST</span>
        </div>
        <div class="system-description">Plataforma Integral de Recursos Académicos</div>
      </div>
      
      <div class="card-body">
        <form method="POST" action="/api/login" id="loginForm">
          <div class="form-group">
            <label for="email" class="form-label">
              <i class="fas fa-envelope"></i>
              Correo Institucional
            </label>
            <div class="input-icon">
              <i class="fas fa-user"></i>
            </div>
            <input 
              type="email" 
              id="email" 
              name="email" 
              class="form-control" 
              placeholder="usuario@institucion.edu" 
              required
              autocomplete="username"
            >
          </div>
          
          <div class="form-group">
            <label for="password" class="form-label">
              <i class="fas fa-lock"></i>
              Contraseña
            </label>
            <div class="password-container">
              <div class="input-icon">
                <i class="fas fa-key"></i>
              </div>
              <input 
                type="password" 
                id="password" 
                name="password" 
                class="form-control" 
                placeholder="••••••••" 
                required
                autocomplete="current-password"
              >
              <button type="button" class="toggle-password" id="togglePassword">
                <i class="far fa-eye"></i>
              </button>
            </div>
          </div>
          
          
          <button type="submit" class="btn btn-primary" style="margin-top: 1.75rem;">
            <i class="fas fa-sign-in-alt"></i>
            Iniciar Sesión
          </button>
        </form>
      </div>
      
      <div class="card-footer">
        <a href="mailto:soporte.merinomiki472@gmail.com">
          <i class="fas fa-headset"></i>
          Soporte Técnico
        </a>
      </div>
    </div>
  </section>

  <script>
    // Toggle password visibility
    document.getElementById('togglePassword').addEventListener('click', function() {
      const passwordInput = document.getElementById('password');
      const icon = this.querySelector('i');
      
      if (passwordInput.type === 'password') {
        passwordInput.type = 'text';
        icon.classList.remove('fa-eye');
        icon.classList.add('fa-eye-slash');
      } else {
        passwordInput.type = 'password';
        icon.classList.remove('fa-eye-slash');
        icon.classList.add('fa-eye');
      }
    });
    
    // Form submission handling with animation
    document.getElementById('loginForm').addEventListener('submit', function(e) {
      e.preventDefault();
      const submitBtn = this.querySelector('button[type="submit"]');
      
      // Add loading animation
      submitBtn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Ingresando...';
      submitBtn.disabled = true;
      
      // Simulate API call
      setTimeout(() => {
        submitBtn.innerHTML = '<i class="fas fa-check"></i> Acceso concedido';
        
        // Redirect after success (simulated)
        setTimeout(() => {
          window.location.href = '/home';
        }, 1000);
      }, 1500);
    });
  </script>
</body>
</html>